<h1>My Dexes</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @dexes.each do |dex| %>
      <tr>
        <td><%= dex.description %></td>
        <% if dex.user == current_user %>
          <td><%= link_to 'Show', dex %></td>
          <td><%= link_to 'Edit', edit_dex_path(dex) %></td>
          <td><%= link_to 'Destroy', dex, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% if user_signed_in? %>
  <%= link_to 'New Dex', new_dex_path %>
<% end %>
